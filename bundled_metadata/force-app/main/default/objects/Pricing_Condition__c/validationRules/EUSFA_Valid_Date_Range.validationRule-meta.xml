<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>EUSFA_Valid_Date_Range</fullName>
    <active>true</active>
    <description>Blocks save when Valid From is after Valid To.</description>
    <errorConditionFormula>AND(
  NOT(ISBLANK(Valid_From__c)),
  NOT(ISBLANK(Valid_To__c)),
  Valid_From__c &gt; Valid_To__c
)</errorConditionFormula>
    <errorMessage>Valid From date must be on or before Valid To date.</errorMessage>
</ValidationRule>
