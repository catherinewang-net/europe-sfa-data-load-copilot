<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Valid_from_Date__c</fullName>
    <defaultValue>Today()</defaultValue>
    <description
  >Start date of the store-to-route assignment validity period. Together with Valid to Date, defines the active window. Overlapping date ranges for the same store and route are not allowed.</description>
    <inlineHelpText
  >The date this routing assignment becomes effective. Overlapping date ranges for the same store and route are blocked — ensure this does not conflict with existing records.</inlineHelpText>
    <label>Valid from Date</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Date</type>
</CustomField>
